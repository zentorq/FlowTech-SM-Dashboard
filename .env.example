# Neon PostgreSQL connection string (Neon dashboard -> your project -> Connection Details)
# NEVER commit the real value. On Vercel, set this in Project -> Settings -> Environment Variables.
DATABASE_URL="postgresql://USER:PASSWORD@HOST.neon.tech/DBNAME?sslmode=require"
